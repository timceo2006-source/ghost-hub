local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Ghost Hub",
    Icon = "door-open",
    Author = "TIM",
    Folder = "MyGhostHub",
    Size = UDim2.fromOffset(580, 460),
    Theme = "Dark",
})

local MainTab = Window:Tab({ Title = "Tab Title", Icon = "bird" })

-- [ส่วนที่แก้ไข 1: ตัวแปรควบคุม]
local autoFarmActive = false
local loopTask = nil

-- ฟังก์ชันที่ใช้ในลูป (ปรับปรุงให้รองรับการเกิดใหม่)
loopTask = task.spawn(function()
    while autoFarmActive do
        local Player = game:GetService("Players").LocalPlayer
        local Character = Player.Character
        
        -- เช็คตัวละครและอาวุธ (เหมือนเดิม)
        if not Character or not Character:FindFirstChild("Humanoid") or Character.Humanoid.Health <= 0 then
            task.wait(2); continue
        end
        local tool = Character:FindFirstChild("Combat") or Player.Backpack:FindFirstChild("Combat")
        if not tool then task.wait(10); if Character:FindFirstChild("Humanoid") then Character.Humanoid.Health = 0 end; continue end

        local TargetFolder = workspace:FindFirstChild("NPC Zones") and workspace["NPC Zones"]:FindFirstChild("Starter Island") and workspace["NPC Zones"]["Starter Island"]:FindFirstChild("NPCS")
        
        if TargetFolder then
            local currentLevel = Player.PlayerData.Experience.Level.Value
            local _, targetName = getQuestInfo()
            
            -- ตรวจสอบเควส
            if Player.Quest.GiverName.Value == "" then acceptQuest(select(1, getQuestInfo())) end

            -- [ส่วนที่แก้ไข]: ระบบเช็คเป้าหมายเดิมก่อนหาใหม่
            local needsNewTarget = true
            if lockedTarget and lockedTarget:FindFirstChild("Humanoid") and lockedTarget.Humanoid.Health > 0 then
                -- ถ้าเป้าหมายเดิมยังมีชีวิตและอยู่ในระยะ ให้ล็อคเป้าเดิมไว้
                needsNewTarget = false
            end

            -- หาเป้าหมายใหม่เฉพาะเมื่อไม่มีเป้าหมายเดิม
            if needsNewTarget then
                lockedTarget = nil
                for _, obj in pairs(TargetFolder:GetChildren()) do
                    if obj:FindFirstChild("Humanoid") and obj.Humanoid.Health > 0 and string.match(obj.Name, "^" .. targetName) then
                        local isBoss = string.find(string.lower(obj.Name), "boss") ~= nil
                        if (currentLevel < 25 and not isBoss) or (currentLevel >= 25 and isBoss) then
                            lockedTarget = obj
                            break
                        end
                    end
                end
            end

            -- โจมตี
            if lockedTarget and lockedTarget:FindFirstChild("HumanoidRootPart") then
                Character.Humanoid:ChangeState(Enum.HumanoidStateType.Physics)
                Character.HumanoidRootPart.CFrame = CFrame.lookAt(lockedTarget.HumanoidRootPart.Position + Vector3.new(0, 7, 0), lockedTarget.HumanoidRootPart.Position)
                Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero
                
                if not Character:FindFirstChild("Combat") then Character.Humanoid:EquipTool(tool) end
                tool:Activate()
            else
                lockedTarget = nil -- ถ้าหาไม่เจอให้ล้างค่า
                Character.Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
        task.wait(0.2) -- ลดเวลา wait เพื่อให้ตอบสนองไวขึ้น
    end
end)
})




-- 1. สร้าง Tab (ลองใช้ :Tab ถ้า :AddTab แล้ว Error)
local Tab = Window:Tab({
    Title = "Stats",
    Icon = "bird"
})

-- 2. สร้าง Dropdown (ต้องใช้ :AddDropdown)
local Dropdown = Tab:AddDropdown({
    Title = "Stats",
    Desc = "เลือกสถานะที่ต้องการอัพ",
    Values = { "Combat", "Defense", "Sword", "Fruit" },
    Value = { "Combat" },
    Multi = true,
    AllowNone = true,
    Callback = function(option) 
        -- ฟังก์ชันจะทำงานเมื่อเลือก
        print("Categories selected: " .. game:GetService("HttpService"):JSONEncode(option)) 
    end
})
