local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Ghost Hub",
    Icon = "door-open",
    Author = "TIM",
    Folder = "MyGhostHub",
    Size = UDim2.fromOffset(580, 460),
    Theme = "Dark",
})

local MainTab = Window:Tab({ Title = "Tab Title", Icon = "bird" })

-- 1. ประกาศตัวแปรควบคุม
local autoFarmActive = false
local loopTask = nil

-- 2. ฟังก์ชันหลัก (รวมระบบเช็คการตายและเช็คอาวุธ)
local function runFarmLogic()
    local Players = game:GetService("Players")
    local Player = Players.LocalPlayer
    
    loopTask = task.spawn(function()
        while autoFarmActive do
            -- ดึงค่า Character ใหม่ทุกครั้งในลูป (รองรับการเกิดใหม่)
            local Character = Player.Character
            if not Character or not Character:FindFirstChild("Humanoid") or Character.Humanoid.Health <= 0 then
                task.wait(2)
                continue
            end

            -- เช็คอาวุธ (ถ้าไม่มีในมือและกระเป๋า ให้รอ 10 วิ ถ้ายังไม่มีให้รีตัว)
            local tool = Character:FindFirstChild("Combat") or Player.Backpack:FindFirstChild("Combat")
            if not tool then
                task.wait(10)
                if Character:FindFirstChild("Humanoid") then Character.Humanoid.Health = 0 end
                continue
            end

            -- ค้นหา Path ในลูป (แก้ปัญหาบางเกมรีเซ็ต Workspace)
            local TargetFolder = workspace:FindFirstChild("NPC Zones") and workspace["NPC Zones"]:FindFirstChild("Starter Island") and workspace["NPC Zones"]["Starter Island"]:FindFirstChild("NPCS")
            
            if TargetFolder then
                local currentLevel = Player.PlayerData.Experience.Level.Value
                
                -- เลือกเควสและชื่อเป้าหมาย
                local qName, tName = "Level 1", "Thief"
                if currentLevel >= 25 then qName, tName = "Level 25", "Bandit Boss"
                elseif currentLevel >= 10 then qName, tName = "Level 10", "Bandit" end

                -- รับเควส
                if Player.Quest.GiverName.Value == "" then
                    local NpcWorkspace = workspace:FindFirstChild("Npc_Workspace")
                    local QuestGiver = NpcWorkspace and NpcWorkspace:FindFirstChild("QuestGivers") and NpcWorkspace.QuestGivers:FindFirstChild("1")
                    local questFunc = Player:FindFirstChild("PlayerGui") and Player.PlayerGui:FindFirstChild("QuestGui") and Player.PlayerGui.QuestGui:FindFirstChild("QuestFunction")
                    if questFunc then pcall(function() questFunc:InvokeServer(QuestGiver, qName) end) end
                end

                -- ค้นหาเป้าหมาย
                local lockedTarget = nil
                for _, obj in pairs(TargetFolder:GetChildren()) do
                    if obj:FindFirstChild("Humanoid") and obj.Humanoid.Health > 0 and string.match(obj.Name, "^" .. tName) then
                        local isBoss = string.find(string.lower(obj.Name), "boss") ~= nil
                        -- Logic: < 25 ตีมอนปกติ, >= 25 ตีเฉพาะ Boss
                        if (currentLevel < 25 and not isBoss) or (currentLevel >= 25 and isBoss) then
                            lockedTarget = obj
                            break
                        end
                    end
                end

                -- ล็อคตำแหน่งและตี
                if lockedTarget and lockedTarget:FindFirstChild("HumanoidRootPart") then
                    Character.Humanoid:ChangeState(Enum.HumanoidStateType.Physics)
                    Character.HumanoidRootPart.CFrame = CFrame.lookAt(lockedTarget.HumanoidRootPart.Position + Vector3.new(0, 7, 0), lockedTarget.HumanoidRootPart.Position)
                    Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero
                    
                    if not Character:FindFirstChild("Combat") then Character.Humanoid:EquipTool(tool) end
                    tool:Activate()
                end
            end
            task.wait(0.5)
        end
    end)
end

-- 3. ส่วน Toggle ใน UI
MainTab:Toggle({
    Title = "AutoFarm",
    Desc = "AutoFarmLevel",
    Value = false,
    Callback = function(state)
        autoFarmActive = state
        if state then
            runFarmLogic()
        else
            if loopTask then 
                task.cancel(loopTask) 
                loopTask = nil 
            end
            local char = game:GetService("Players").LocalPlayer.Character
            if char and char:FindFirstChild("Humanoid") then
                char.Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    end
})




-- 1. สร้าง Tab (ลองใช้ :Tab ถ้า :AddTab แล้ว Error)
local Tab = Window:Tab({
    Title = "Stats",
    Icon = "bird"
})

-- 2. สร้าง Dropdown (ต้องใช้ :AddDropdown)
local Dropdown = Tab:AddDropdown({
    Title = "Stats",
    Desc = "เลือกสถานะที่ต้องการอัพ",
    Values = { "Combat", "Defense", "Sword", "Fruit" },
    Value = { "Combat" },
    Multi = true,
    AllowNone = true,
    Callback = function(option) 
        -- ฟังก์ชันจะทำงานเมื่อเลือก
        print("Categories selected: " .. game:GetService("HttpService"):JSONEncode(option)) 
    end
})
